import React from 'react';

export function ReactUnused({ title, description }) {
  return (
    <div className="react-unused">
      <h2>{title}</h2>
      <p>{description}</p>
    </div>
  );
}

export default ReactUnused;
